[project]
name = "dash-on-flask"
version = "1.16.1"
description = "Dash apps integrated with Flask for CAVE neuroscience visualization"
requires-python = ">=3.10"
dependencies = [
    "flask",
    "dash",
    "dash-connectivity-viewer>=2.3.2",
    "middle-auth-client>=3.11.1",
    "cell-search",
    "uwsgi",
]

[tool.uv]
# Not a distributable package — just a web app.
package = false


[dependency-groups]
dev = [
    "bump-my-version",
]

[tool.bumpversion]
current_version = "1.16.1"
commit = true
tag = true

[[tool.bumpversion.files]]
filename = "app/webapp.py"

[[tool.bumpversion.files]]
filename = "pyproject.toml"
search = 'version = "{current_version}"'
replace = 'version = "{new_version}"'

[[tool.bumpversion.files]]
filename = "uv.lock"
search = """
name = "dash-on-flask"
version = "{current_version}"
"""
replace = """
name = "dash-on-flask"
version = "{new_version}"
"""
